from setuptools import setup, find_packages

setup(
    name='ultralytics',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
    include_package_data=True,
    description='Ultralytics YOLO models and utilities',
)
